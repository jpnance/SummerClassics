extends layout.pug

block title
	+title('Picks')

block content
	table.table
		tr
			th

			each i in [1, 2, 3, 4, 5, 6, 7]
				th.text-nowrap.px-2
					span.d-none.d-md-inline Game #{i}
					span.d-md-none G#{i}

		each team in teams
			tr
				th.team.text-nowrap.text-right.py-2
					span.team-name #{team.teamName}
					br
					span.record
						+record(team.classic)

				if team.classic
					each i in [0, 1, 2, 3, 4, 5, 6]
						td.text-center
							if team.classic.picks[i]
								- var game = team.classic.picks[i];
								a(href='/schedule/' + dateFormat(game.startTime, 'yyyy-mm-dd'))
									if !game.isFinal()
										| #{game.opponent.team.abbreviation}
									else if !game.opponent.winner
										strong #{game.opponent.team.abbreviation}
									else
										strike #{game.opponent.team.abbreviation}
